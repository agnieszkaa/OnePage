@model IEnumerable<BaseFirst.Contact>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (BaseFirst.Contact item in this.Model)
{
    @item.NAme;
    <p> </p>
    @item.LastNAme;
    <p><br/></p>

    @Html.ActionLink("Dalete","Dalete", new { id = item.Id })
    <br />
    <br />
}